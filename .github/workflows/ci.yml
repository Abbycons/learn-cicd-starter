name: CD - Notely Docker

# Trigger on push to main branch
on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout the repository
      - name: Checkout code
        uses: actions/checkout@v3

      # 2️⃣ Log in to Docker Hub using GitHub secrets
      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # 3️⃣ Build Docker image
      - name: Build Docker image
        run: docker build --platform linux/amd64 -t abicons/notely:latest .

      # 4️⃣ Push Docker image to Docker Hub
      - name: Push Docker image
        run: docker push abicons/notely:latest

      # 5️⃣ Optional: deploy to Railway (if you add RAILWAY_TOKEN secret)
      # - name: Deploy to Railway
      #   run: railway up --environment production
      #   env:
      #     RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
